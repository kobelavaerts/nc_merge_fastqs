

params {
    input = "test_input/*.fastq.gz"
    output = "results/"
    tosenddir = "tosend/"
    tar_prefix = null
    project = null
}


docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
}